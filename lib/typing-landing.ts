/**
 * sessionStorage key used to hand a language chosen on a /typing/[language]
 * SEO landing page to the home page test. The home page consumes (and
 * clears) it on mount to preselect the language.
 */
export const LANGUAGE_STORAGE_KEY_FALLBACK = "vk-landing-language"
